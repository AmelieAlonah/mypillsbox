   <header class="header">
        <nav class="box navbar p-6" role="navigation" aria-label="main navigation is-flex">
            <div class="navbar-brand is-align-items-center content has-text-centered is-flex is-desktop is-justify-content-space-around">
                <img class="mobile navbar-item is-medium" src="{{ asset('assets/pictures/visual_identity/MyPillsBox_nav.png')}}">
                <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false"
                    data-target="navbarBasicExample">
                    <span class="span-burger" aria-hidden="true"></span>
                    <span class="span-burger" aria-hidden="true"></span>
                    <span class="span-burger" aria-hidden="true"></span>
                </a>
            </div>

            <div id="navbarBasicExample" class="navbar-menu">
                <div class="navbar-start">
                    <a href="{{ path('home')}}" class="navbar-item">
                        Page d'accueil
                    </a>

                    <a class="navbar-item" href="{{ path('medic_research')}}">
                        Rechercher un médicament
                    </a>

                    <a href="{{ path('me')}}" class="navbar-item link_help" id="link_me">
                        Qui sommes-nous ?
                    </a>

                    <div class="navbar-item has-dropdown is-hoverable dropdown">
                        <a class="navbar-item" aria-haspopup="true" aria-controls="dropdown-menu">
                            <span>Un peu plus ?</span>
                            <span class="icon is-small">
                                <i class="fas fa-angle-down" aria-hidden="true"></i>
                            </span>
                        </a>

                        <div class="navbar-dropdown dropdown-content">
                            <a href="{{path('faq_browse')}}" class="navbar-item dropdown-item" id="link_faq">
                                La Foire Aux Questions
                            </a>
                            <a href="{{path('news_browse')}}" class="navbar-item dropdown-item">
                                Les news !
                            </a>
                            <a href="{{ path('contact')}}" class="navbar-item dropdown-item">
                                Me contacter
                            </a>
                        </div>
                    </div>

                    {# IF ADMIN CONNECTED #}
                    {% if is_granted('ROLE_ADMIN') %}
                        {{ include('@LAYOUT/_back_subnav.html.twig')}}
                    {% endif %}
                    

                </div>

                

                <div class="navbar-end">
                    <div class="navbar-item">
                        <div class="buttons is-flex is-justify-content-center">
                            
                            {% if is_granted('ROLE_USER') %}
                            
                                <div class="button is-primary">
                                    <a href="{{ path('app_logout')}}"><strong>Me déconnecter</strong></a>
                                </div>

                                {% else %}

                                <div class="button is-primary">
                                    <a href="{{ path('account_add')}}"><strong>M'inscrire</strong></a>
                                </div>
                                <div class="button is-primary">
                                    <a href="{{ path('app_login')}}"><strong>Me connecter</strong></a>
                                </div>   

                            {% endif %}

                        </div>
                    </div>

                    
                </div>
            </div>
        </nav>
    </header>